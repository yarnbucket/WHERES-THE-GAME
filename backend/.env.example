# Example environment variables for Where's the Game

DB_USER=postgres
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wheres_the_game
backend/.gitignore
# Ignore real environment variables
.env
